pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                // Checkout source code
                echo "Hello world"
            }
        }

        stage('Backend Build and Deploy') {
            steps {
                // Build the backend
                sh script 'mvn clean install'
            }
        }
    }
}
